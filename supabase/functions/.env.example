# Copy to supabase/functions/.env for `supabase functions serve --env-file supabase/functions/.env`
# Secret used to sign portal session tokens (falls back to the service role key if absent).
PORTAL_TOKEN_SECRET=change-me-to-a-long-random-string
